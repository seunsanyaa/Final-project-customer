import { Mybookings } from "@/components/general/bookings/mybookings";

// Define the Bookingspage component as the default export
export default function Bookingspage() {
  return (
    // Render the Mybookings component
    <Mybookings />
  );
}

// Note: Consider adding props or context if needed for customization


